namespace ASM.Data
{
    /// <summary>Filter a trait target by the trait's inheritability.</summary>
    public enum InheritableFilter
    {
        Both,
        Inheritable,
        NonInheritable
    }
}
